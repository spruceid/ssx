{
  "extends": "../../tsconfig.json",
  "include": [
    "src/**/*"
  ],
  "exclude": [
    "node_modules/",
    "dist/**/*"
  ],
  "compilerOptions": {
    "outDir": "./dist", /* Specify an output folder for all emitted files. */
  }
}
